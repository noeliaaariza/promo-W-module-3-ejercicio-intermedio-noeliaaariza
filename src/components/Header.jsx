function Header() {
  return (
    <header>
      <h1>Country Info App</h1>
      <p>
        Explore information about countries, capitals and flags. `Maybe` add new
        countries and filter through the list
      </p>
    </header>
  );
}

export default Header;
